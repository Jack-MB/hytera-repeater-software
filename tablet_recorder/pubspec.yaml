name: tablet_recorder
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  audioplayers: ^6.7.1
  flutter:
    sdk: flutter
  path_provider: ^2.1.6
  permission_handler: 11.3.1
  record: ^6.2.1
  share_plus: ^13.3.0
  shelf: ^1.4.2
  wakelock_plus: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
